version: '3.1'

services:
  app:
    build: .
    ports:
      - 5000:5000
    volumes:
      - .:/usr/src/app
    environment:
      MONGO_URI: mongodb://db:27017/
    depends_on:
      - db
  db:
    image: mongo
    restart: always
    ports:
      - 27017:27017
